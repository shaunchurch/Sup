apply plugin: 'android'

android {
    compileSdkVersion 19
    buildToolsVersion "19.0.1"

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 19
        versionCode 4
        versionName "0.1.6"
    }

    signingConfigs {
        debug {
            storeFile file("/Users/shaun/.android/debug.keystore")
        }

        myConfig {
            storeFile file("/Users/shaun/.android/keystore.keystore")
            storePassword "nf7fEJXMcTWhWsz3tknH"
            keyAlias "fistbump"
            keyPassword "nf7fEJXMcTWhWsz3tknH"
        }
    }


    buildTypes {
        release {
            runProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
            signingConfig signingConfigs.myConfig

        }

        debug {
            runProguard false
            signingConfig signingConfigs.debug
        }
    }
}

dependencies {
    compile 'com.android.support:support-v13:+'
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
